-- Monitor layout ported from legacy hyprland.conf

hl.monitor({ output = MONITOR1, mode = "preferred", position = "auto", scale = "1.25" })
hl.monitor({ output = MONITOR2, mode = "preferred", position = "auto", scale = "1" })
